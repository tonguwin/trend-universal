import React from 'react';
import Box from 'components/box';
import Layout from 'components/layout';
import Head from 'components/head';

const Issues = () => (
  <Layout>
    <Head pageTitle= 'Issues' />
    <Box>Not found.</Box>
  </Layout>
);

export default Issues;
